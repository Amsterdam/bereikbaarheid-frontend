services:
  web-dev:
    build:
      context: .
      args:
        BUILD_ENV: local
        NGINX_CONF: nginx.local.conf
    environment:
      TZ: Europe/Amsterdam
    ipc: host
    ports:
      - '3000:8080'
